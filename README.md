### Bank Mode Text
- Replaces the Insert/Swap mode icon with text ("Insert" or "Swap")
- Replaces the Note mode icon with text ("Note")
- Optional "Show Active Rearrange Mode" setting that displays the currently active rearrange mode in green below the button on hover
